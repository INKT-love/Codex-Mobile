# Android App

Native Kotlin + Jetpack Compose client for Codex Mobile.

The current workspace does not include a local Java/Android SDK toolchain, so this project is scaffolded but not compiled locally yet.
