# Android App

This directory is reserved for the Kotlin + Jetpack Compose app.

The Android project will be created after the TypeScript relay server, shared protocol package, and PC Agent have a working local loop.
